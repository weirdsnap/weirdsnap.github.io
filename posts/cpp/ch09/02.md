# placement new 与显式析构

在已分配的内存上构造对象（不分配新内存），常用于内存池、容器（如 `std::vector`）。需手动调用析构函数，且不能对该指针使用 `delete`。

**延伸点**：`std::vector` 的 `push_back` / `emplace_back` 内部使用 placement new 在已分配的容量空间中构造对象，配合 `std::uninitialized_copy` 等算法实现异常安全的批量构造。